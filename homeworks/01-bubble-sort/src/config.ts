type Config = {
  MIN_COLS: number;
  MAX_COLS: number;
  FIELD_HEIGHT: number;
  SORTING_DELAY: number;
};

const CONFIG: Config = {
  MIN_COLS: 20,
  MAX_COLS: 30,
  FIELD_HEIGHT: 200,
  SORTING_DELAY: 100,
};

export default CONFIG;
